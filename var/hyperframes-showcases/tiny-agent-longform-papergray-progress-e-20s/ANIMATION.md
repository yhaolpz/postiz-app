# Animation plan

- 底部进度：八个章节段全部常驻显示，宽度沿用正式长视频的真实章节时长比例；当前章节采用 `stat-bars-and-fills` 的 `scaleX` 满高灰色填充，`transform-origin` 固定在左侧，章节文字固定在填充层上方。
- 角色和道具入场：采用 `spring-pop-entrance` 的平滑 `power3.out` 版本，不使用弹跳；角色只做轻微上移与透明度变化，道具使用小幅缩放进入。
- 场景切换：沿用已确认长视频基线的短距离压缩与横向遮罩揭示，切换窗口为 `0.54s`。
- 字幕：按原旁白时间逐条淡入淡出，字幕框自身保持固定。
- 最后一条完整旁白在 `18.458s` 结束，画面持有到 `20s`，不落黑场。
