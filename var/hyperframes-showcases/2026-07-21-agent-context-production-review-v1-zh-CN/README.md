# 正式生产规则审核片

本工程不是独立样片模板，而是候选正式首分钟生产配置的短时审核输出。`production-profile.json` 是唯一视觉与开头规则事实源，`build.mjs` 会校验首句、固定价值句、封面切点来源和单主体数量。封面在第一句真实可听结束后的首个 30fps 帧切换，避免非帧对齐事件产生空白过渡帧。

当前状态为 `review-only-not-promoted`：不覆盖既有工程、不发布、不写入每日自动化基线。用户确认后，完整双语长视频应消费同一份生产配置。
