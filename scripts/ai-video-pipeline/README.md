# Video production moved

Active video production now lives in:

```text
/Volumes/SSD/Workspace/seek/apps/video-production
```

This Postiz repository only validates and imports immutable Seek release
bundles, then performs separately authorized platform distribution. See
[`../video-distribution/README.md`](../video-distribution/README.md).

Historical renders and archives under `var/` remain in place and are not active
production inputs.
